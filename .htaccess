Options -Indexes
<IfModule mod_rewrite.c>
  RewriteEngine On
  RewriteBase /dev/symfony_mixed_media_app
  
  # 1) Already in /public → do nothing (another .htaccess in /public can handle routing)
  RewriteRule ^public/ - [L]

  # 2) Everything else → redirect to /public/<same-path> (preserve query string)
  RewriteRule ^(.*) public/ [QSA,R=302,L]
  # Use R=301 once you're sure, to make it permanent
</IfModule>